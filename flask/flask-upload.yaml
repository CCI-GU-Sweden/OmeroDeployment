apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flask-upload
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: shared-standard
  volumeMode: Filesystem

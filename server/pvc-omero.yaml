apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omero
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: shared-standard
  volumeMode: Filesystem


apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: server-omero
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: 
  volumeMode: Filesystem


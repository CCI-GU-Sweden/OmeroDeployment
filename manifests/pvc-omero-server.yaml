apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opt-omero
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: shared-standard
  volumeMode: Filesystem


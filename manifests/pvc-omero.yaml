apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omero
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: block-standard
  volumeMode: Filesystem


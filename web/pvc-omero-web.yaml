apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omero-web
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: shared-standard
  volumeMode: Filesystem
